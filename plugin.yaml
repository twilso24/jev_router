name: jev_router
title: Jev Router
description: Standalone TypeSafe Jev-based auto-routing for Agent Zero. Real-call
  telemetry, state-aware auto-tune, and live WebUI panel. No external plugin
  dependencies required.
version: 0.3.0
author: Agent Zero Plugin (jev_router)
license: MIT
always_enabled: false
settings_sections:
  - external
per_project_config: true
per_agent_config: true
